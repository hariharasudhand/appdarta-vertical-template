# Project Observability Common

This is a loosely coupled vertical common. Use commons for shared schedulers, loggers, adapters, and handlers that multiple subdomains can reference without forcing a single tightly bound runtime unit.
